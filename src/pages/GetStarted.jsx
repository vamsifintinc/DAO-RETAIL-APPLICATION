import ApplicationLayout from '../layouts/ApplicationLayout'
import { GET_STARTED } from '../constants/pageConstants'

const GetStarted = () => {
  return (
    <ApplicationLayout pageConstants={GET_STARTED}>
      {/* Right side content (can be empty for now) */}
      {/* <div>Get Started Page</div> */}
    </ApplicationLayout>
  )
}

export default GetStarted
